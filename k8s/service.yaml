apiVersion: v1
kind: Service
metadata:
  name: tp2-api
  namespace: tassioalmeida
spec:
  selector:
    app: tp2-api
  ports:
  - port: 50028
    targetPort: 50028
    nodePort: 30502
  type: NodePort
